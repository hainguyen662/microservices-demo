apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.cartservice.name }}-hpa
  namespace: {{ .Release.Namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.cartservice.name }}
  minReplicas: {{ .Values.cartservice.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.cartservice.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.cartservice.autoscaling.targetCPUUtilizationPercentage }}
