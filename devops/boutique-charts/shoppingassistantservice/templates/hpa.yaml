apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.shoppingassistantservice.name }}-hpa
  namespace: {{ .Release.Namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.shoppingassistantservice.name }}
  minReplicas: {{ .Values.shoppingassistantservice.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.shoppingassistantservice.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.shoppingassistantservice.autoscaling.targetCPUUtilizationPercentage }}
