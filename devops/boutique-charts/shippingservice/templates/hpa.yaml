apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.shippingservice.name }}-hpa
  namespace: {{ .Release.Namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.shippingservice.name }}
  minReplicas: {{ .Values.shippingservice.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.shippingservice.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.shippingservice.autoscaling.targetCPUUtilizationPercentage }}
