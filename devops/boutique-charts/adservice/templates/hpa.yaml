apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.adservice.name }}-hpa
  namespace: {{ .Release.Namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.adservice.name }}
  minReplicas: {{ .Values.adservice.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.adservice.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.adservice.autoscaling.targetCPUUtilizationPercentage }}
